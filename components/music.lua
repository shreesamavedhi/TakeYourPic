function LoadMusic()
    -- mainLoop = love.audio.newSource("music/main.wav", "stream")
    -- goodScoreWav = love.audio.newSource("music/goodScore.wav", "static")
    -- MasterVolume = 0.5 -- Maximum volume for all sounds
    -- MusicVolume = 1
    -- SoundVolume = 0.75
    -- goodScoreWav:setVolume(masterVolume * effectVolume)
    -- mainLoop:setVolume(masterVolume * musicVolume)
end

function UpdateMusic(dt)
    -- if not mainLoop:isPlaying() then
	-- 	love.audio.play(mainLoop)
	-- end
end